<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CarMax"
             x:Class="CarMax.MainPage">

    <MasterDetailPage.Master>
        <ContentPage Title="Menu" Padding="10">
            <ContentPage.Content>
                <StackLayout Margin="5" Orientation="Vertical">



                    <ListView  ItemsSource="{Binding MenuList}" ItemSelected="OnMenuItemSelected">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ImageCell Text="{Binding Text}"   ImageSource="{Binding ImagePath}">
                                </ImageCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                </StackLayout>

            </ContentPage.Content>
        </ContentPage>
    </MasterDetailPage.Master>

    <MasterDetailPage.Detail>
        <NavigationPage></NavigationPage>
    </MasterDetailPage.Detail>


</MasterDetailPage>
